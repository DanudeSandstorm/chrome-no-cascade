{
	"manifest_version": 2,

	"name": "Chrome no Cascade",
	"description": "Prevents chrome from cascading new windows.",
	"version": "0.2",

	"permissions": [
		"tabs"
	],

	"background": {
		"scripts": ["background.js"]
	}

}
