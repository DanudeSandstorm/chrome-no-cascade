{
	"manifest_version": 2,

	"name": "Chrome no Cascade",
	"description": "Prevents chrome from cascading new windows.",
	"version": "0.3",

	"permissions": [
		"activeTab"
	],

	"background": {
		"scripts": ["background.js"]
	}

}
